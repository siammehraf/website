export const metadata = {
  title: 'Newsletter | Siam Mehraf',
  description: 'Join 100+ readers for insights on productivity, business, and intentional living.',
};

export default function NewsletterLayout({ children }) {
  return <>{children}</>;
}
